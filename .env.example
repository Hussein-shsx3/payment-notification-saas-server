# Server
NODE_ENV=development
PORT=5000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/payment-notification-saas

# JWT
JWT_ACCESS_SECRET=your-super-secret-access-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
JWT_ACCESS_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=30d

# Verification email (Gmail SMTP — Google App Password, not your normal password)
# 1) Google Account → Security → turn on 2-Step Verification
# 2) App passwords → create one for "Mail" → copy the 16-character password here (spaces optional)
GMAIL_USER=you@gmail.com
GMAIL_APP_PASSWORD=
# Optional display name / from header; defaults to "APP_NAME <GMAIL_USER>"
MAIL_FROM=
API_PUBLIC_URL=http://localhost:5000/api
APP_NAME=Payment Notify

# Frontend URL
FRONTEND_URL=http://localhost:3000
# Admin dashboard URL
ADMIN_URL=http://localhost:5173

# Keep-alive / sleep monitoring
# IMPORTANT: for Render, set KEEP_ALIVE_URL to your public health URL
# e.g. https://your-service.onrender.com/health
KEEP_ALIVE_URL=
